services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=inboxview_app_db'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432'
  mailhog:
    image: mailhog/mailhog
    ports:
      - '1025:1025'
      - '8025:8025'